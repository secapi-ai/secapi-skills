{
  "name": "Analyze Company in Context",
  "summary": "Build a compact issuer brief from company, earnings, factor, and macro context.",
  "description": "Use before deeper research when you need to identify the business, filing, factor, or macro facts that change the next question.",
  "status": "active",
  "tags": ["company", "security", "context", "factors", "macro"],
  "triggers": ["company analysis", "security overview", "allocator brief", "issuer context", "company bundle", "company brief", "investor context pack"],
  "requiredEndpoints": [
    "GET /v1/companies/overview",
    "GET /v1/companies/segments",
    "GET /v1/entities/resolve",
    "GET /v1/intelligence/security",
    "GET /v1/intelligence/earnings-preview",
    "GET /v1/factors/exposures",
    "GET /v1/macro/regimes"
  ],
  "tier": "core",
  "schemaVersion": 1
}
